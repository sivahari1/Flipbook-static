version: 1
frontend:
  phases:
    build:
      commands:
        - echo "🚀 Static HTML Deployment"
        - echo "✅ No build needed - serving static HTML"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'